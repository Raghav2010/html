/**
 * 1. npm install -g jsdoc
 * 2. jsdoc filename.js tt
 * this function takes two params and returns the sum
 * @param {number} a - the first param
 * @param {number} b  - th second param
 * @param {number} result - the sum of two numbers
*/
function sum(a,b)
{
    return a+b;
}